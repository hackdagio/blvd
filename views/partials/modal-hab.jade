.modal-header
	.glyphicon-inline
		span.glyphicon.glyphicon-asterisk
	h3.modal-title.text-uppercase
		| Indicadores para la habilidad <strong>{{ title }}_</strong>

.modal-body.kaizen-modal
	table.table.table-hover.kaizen-modal-table
		thead
			tr
				th(ng-repeat='ind in kpi') {{ ind | uppercase }}
		tbody
			tr
				th(ng-repeat='item in items') {{ item[0] }}%

.modal-footer
	button.btn.btn-primary(ng-click='ok()') [ OK _